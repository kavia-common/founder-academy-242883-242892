export const API_BASE_URL =
  process.env.EXPO_PUBLIC_API_BASE_URL ??
  "http://localhost:3001";

/**
 * Backend API base URL.
 * NOTE: In Expo web this should be the full https URL to the running backend.
 */
