# FouGuide Frontend Environment Variables
# Copy to .env (already present in runtime) and fill values.

# Base URL of the backend API (FastAPI).
# Example: https://your-backend.example.com
EXPO_PUBLIC_API_BASE_URL=

# Optional: Site URL for auth email redirects (if backend supports it later).
EXPO_PUBLIC_SITE_URL=
